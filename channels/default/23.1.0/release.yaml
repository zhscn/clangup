schema: clangup.channel/v1
channel: default
version: 23.1.0
release: 1

source:
  url: https://github.com/llvm/llvm-project/releases/download/llvmorg-23.1.0/llvm-project-23.1.0.src.tar.xz
  sha256: ab1f0e3ec52448c33e8782eaf0422504b87c7b016b22514653ee0d8fcee479ff
  patch_series: patches/series.yaml

distribution:
  projects:
    - bolt
    - clang
    - lld
    - clang-tools-extra
  runtimes:
    - compiler-rt
    - libcxx
    - libcxxabi

runtime_delivery:
  libcxx:
    linkage: static
  libcxxabi:
    linkage: static

driver:
  libc: system
  cxx_stdlib: system
  cxx_stdlib_linkage: system
  linker: system
  rtlib: system
  unwindlib: system

targets:
  - os: linux
    arch: x86_64
    triple: x86_64-unknown-linux-gnu
    libc: glibc
    libc_version: "2.17"
    cpu_isa: x86-64
    driver_requirements:
      - gcc-toolchain
      - gnu-linker
      - glibc-devel
    required: true

  - os: linux
    arch: aarch64
    triple: aarch64-unknown-linux-gnu
    libc: glibc
    libc_version: "2.28"
    cpu_isa: armv8-a
    driver_requirements:
      - gcc-toolchain
      - gnu-linker
      - glibc-devel
    required: true

  - os: macos
    arch: aarch64
    triple: arm64-apple-darwin
    min_macos_version: "11.0"
    driver_requirements:
      - xcode-clt
    required: true
    distribution:
      projects:
        - clang
        - clang-tools-extra
      runtimes:
        - compiler-rt
    driver:
      libc: system
      cxx_stdlib: system
      cxx_stdlib_linkage: system
      linker: system
      rtlib: compiler-rt
      unwindlib: system

changelog:
  - release: 1
    date: "2026-08-28"
    summary: Initial LLVM 23.1.0 default channel contract
