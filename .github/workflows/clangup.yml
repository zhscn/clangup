name: clangup

on:
  pull_request:
    paths:
      - .github/workflows/clangup.yml
      - .github/workflows/default-channel.yml
      - .github/workflows/libcxx-channel.yml
      - .github/workflows/clangup-release.yml
      - cmd/**
      - internal/**
      - channels/**
      - go.mod
      - go.sum
  push:
    branches:
      - main
    paths:
      - .github/workflows/clangup.yml
      - .github/workflows/default-channel.yml
      - .github/workflows/libcxx-channel.yml
      - .github/workflows/clangup-release.yml
      - cmd/**
      - internal/**
      - channels/**
      - go.mod
      - go.sum

concurrency:
  group: clangup-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Check formatting
        run: |
          unformatted="$(gofmt -l cmd internal)"
          if [[ -n "$unformatted" ]]; then
            echo "unformatted Go files:" >&2
            echo "$unformatted" >&2
            exit 1
          fi

      - name: Check build scripts
        run: |
          python3 -m py_compile \
            channels/build/run.py \
            channels/build/assemble.py \
            channels/build/publish.py
          python3 -m unittest discover -s channels/build -p 'test_*.py'
          python3 -m unittest discover -s channels/default/22.1.8/build -p 'test_*.py'
          python3 -m unittest discover -s channels/libcxx/22.1.8/build -p 'test_*.py'
          bash -n \
            channels/default/22.1.8/build/build-linux.sh \
            channels/default/22.1.8/build/build-macos.sh \
            channels/default/22.1.8/build/verify-linux.sh \
            channels/default/22.1.8/build/verify-asan-matrix.sh \
            channels/libcxx/22.1.8/build/build-linux.sh \
            channels/libcxx/22.1.8/build/verify-linux.sh \
            channels/libcxx/22.1.8/build/verify-asan-matrix.sh

      - name: Check module files
        run: go mod tidy -diff

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -race ./...

      - name: Build static CLI
        env:
          CGO_ENABLED: "0"
        run: go build -trimpath -o /tmp/clangup ./cmd/clangup
